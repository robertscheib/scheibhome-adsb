# Active Handoff

* [2026-08-10 (Codex GPT-5)]: Moved this independent repository to `/home/robert/projects/adsb-platform/ads-b` alongside (but not merged with) radar-dash. Existing uncommitted handoff and untracked artifacts were preserved.
- [2026-08-10 (Codex GPT-5)]: Investigated reported large water-usage spikes through the Home Assistant history API. The raw cumulative `sensor.water_meter_reading` remained monotonic over seven days with no large negative or implausible jumps. The negative spikes occur on expected daily/weekly utility-meter cycle resets (for example daily values returning to zero at midnight and weekly reset at the configured week boundary). Some positive jumps are derived-sensor catch-up after delayed/sparse cumulative meter updates, not SDR corruption. The raw SDR value and HA cumulative value continue to match after the factor-of-ten conversion.
- [2026-08-10 (Codex GPT-5)]: Read-only Home Assistant value verification confirmed that `sensor.water_meter_reading` is live and internally consistent with the SDR. The raw RTLAMR values and HA state match after the configured factor-of-ten conversion (for example raw `3003762` → HA `300376.2 gal`); HA last-seen was current and the history showed steadily increasing readings. Short `unavailable` intervals recur around periodic integration events and recover within seconds, with no evidence of stale retained data. Physical accuracy still requires comparison with the meter display or utility bill.
- [2026-08-10 (Codex GPT-5)]: Verified Home Assistant after restoring SDR serial `443`: VM 100 is running with `usb0: host=1-2.4.3`, the `rtlamr2mqtt` add-on is started, and its logs show `rtl_tcp` successfully opening serial `443` and publishing meter `1564077862` readings to MQTT (latest observed value `3003739`). This confirms the original HA SDR is staying active after restoration; the earlier shutdown was only the deliberate temporary reassignment for comparison.
- [2026-08-10 (Codex GPT-5)]: Completed the requested long tests on Nooelec serial `37441838` with the tuned 443 MHz antenna. A 30-minute `rtl_433` run at 433.92 MHz decoded zero supported device messages. A subsequent approximately 30-minute listen-only RTLAMR run at the ERT band repeatedly received meter ID `1564077862`, type `R900`; readings included `3003653` and later `3003669`, confirming repeat reception and a changing consumption value. The temporary VM 102 passthrough and scan container were removed; Ultrafeeder and dump978 remained healthy with zero restarts.
- [2026-08-09 (Codex GPT-5)]: Tested the newly attached tuned 443 MHz antenna on Nooelec serial `37441838`. A one-minute passive `rtl_433` run at 433.92 MHz opened the E4000 tuner successfully but decoded no supported 433 MHz device messages. A listen-only RTLAMR run tuned to the approximately 912.6 MHz ERT band then repeatedly detected meter ID `1564077862`, type `R900`, consumption value `3003539`; the same ID was seen again in the longer confirmation run. This demonstrates the SDR/USB path and antenna can receive AMR traffic, although the ID should be confirmed against Robert's own meter before configuring Home Assistant. Temporary passthrough/container were removed; Ultrafeeder and dump978 remained healthy with zero restarts.
- [2026-08-07 (Codex GPT-5)]: Ran a temporary passive `rtl_433` scan against Nooelec serial `37441838` across 315, 345, 433.92, 868.3, and 915 MHz with five-second hopping. The E4000 tuner opened successfully, but approximately one minute without an antenna produced no decoded device messages. The temporary VM 102 passthrough was removed afterward; Ultrafeeder and dump978 remained healthy with zero restarts.
- [2026-08-07 (Codex GPT-5)]: Ran a temporary listen-only RTLAMR scan against the unassigned Nooelec SDR serial `37441838` (selected as device index 0 after confirming the VM's device order). The SDR opened successfully with its E4000 tuner, but approximately one minute without an antenna produced no decoded AMR meter messages. The temporary VM 102 passthrough was removed afterward; Ultrafeeder and dump978 remained healthy with zero restarts.
- [2026-08-07 (Codex GPT-5)]: Migrated the four SDRs visible after the USB-hub move. Proxmox VM 100 (Home Assistant) now passes hub path `1-2.4.3` (RTL2832 serial `443`) plus the Home Assistant ZBT-1 at `usb1: host=10c4:ea60`; VM 102 (ADS-B) now passes `1-2.1.3` (RTL2838 serial `1090`) and `1-2.4.1` (RTL2832 serial `978`). The antenna-less Nooelec SDR serial `37441838` remains intentionally unassigned. Both VMs were stopped/started, then the Proxmox host was fully rebooted. Post-reboot VM 102 sees both assigned SDRs, Ultrafeeder and dump978 are healthy with zero restarts, FR24 is healthy, Ultrafeeder has live ADS-B aircraft data, and Home Assistant returns HTTP 200. Robert clarified that the fifth USB device is the ZBT-1, not a fifth SDR; the four SDRs plus ZBT-1 are accounted for. Proxmox config backups are `/etc/pve/qemu-server/100.conf.bak-sdr-move-20260807-161834` and `/etc/pve/qemu-server/102.conf.bak-sdr-move-20260807-161834`.
- [2026-08-01 (Codex GPT-5)]: Completed the approved AvioADSB integration after Robert regenerated the previously disclosed station token and downloaded a replacement authenticated installer. Securely updated `AVIOADSB_TOKEN` in shared secrets without printing it, added documented AvioADSB placeholders, and exposed Ultrafeeder SBS port 30003 only on receiver loopback for the official host relay. Backed up live Compose as `/opt/adsb/docker-compose.yml.bak-20260801-avioadsb`, validated repository/live Compose, and recreated only Ultrafeeder. Installed and enabled `avioadsb-feeder.service`; its credential-bearing executable is root-owned mode 700. The service authenticated, began streaming to `feeder.avioadsb.org:31003`, remained active with zero restarts, and held an established TCP connection. Ultrafeeder returned healthy and the existing checked feeders remained running. AvioADSB was also added to radar-dash as a force-enabled static audit entry because it runs as a remote systemd service. adsb.exposed remains excluded. Nothing was committed or pushed.
- [2026-08-01 (Codex GPT-5)]: Added ADSBiq as a direct ADS-B and MLAT destination after Robert approved the new feeder integration. Generated a dedicated UUID, registered station `KLOT9`, and stored the returned contributor API token only in shared/live secret files. Rotated the initial feeder identity and replacement API token after diagnostic output disclosed the first identifier; tracked diffs contain neither current credential. Added the documented Beast Reduced Plus and privacy-enabled MLAT connections to the repository and live Compose files, validated Compose, and recreated only Ultrafeeder. Both rotated ADSBiq connections established successfully, contributor API authentication passed, and Ultrafeeder plus dump978, PiAware, FR24, PlaneFinder, ADSBHub, AirNav RadarBox, RadarVirtuel, Plane.watch, and OpenSky are healthy with zero restarts; 1090 and UAT JSON sources are available. Live backups are `/opt/adsb/docker-compose.yml.bak-20260801-adsbiq` and `/opt/adsb/.env.bak-20260801-adsbiq`. Robert completed AvioADSB account/receiver registration and downloaded the authenticated Linux installer. Inspection confirmed it is a bounded Bash/systemd SBS relay to `feeder.avioadsb.org:31003`, but its embedded token differed from the saved value and was disclosed in diagnostic output. Installation stopped before any AvioADSB file/service was created; the downloaded script is owner-only mode 600, and AvioADSB remains paused pending dashboard token regeneration and a replacement download. The eventual host service will also need loopback-only access to Ultrafeeder's SBS port 30003. adsb.exposed was intentionally excluded because it has no direct feeder ingest.
- [2026-07-31 (Codex GPT-5)]: Updated the live FR24 UAT sharing key after Robert replaced `FR24KEY_UAT` in shared `~/projects/secrets.env`. Backed up live `/opt/adsb/.env` as `/opt/adsb/.env.bak-20260731-123425-fr24-uat-key`, changed only `FR24KEY_UAT`, validated Compose, and recreated only `fr24`. FR24 accepted the new key as dedicated UAT station `KLOT61`; the UAT process connected to `dump978:30978`, downloaded its configuration, connected to FR24 over UDP, and reported `working`. Final validation: shared/live key values match; primary station `KLOT9` and UAT station `KLOT61` are working; status ports 8754 and 8755 return HTTP 200; FR24 and `dump978` are healthy with zero restarts; and the UAT snapshot contained 31 aircraft seen within 60 seconds. No repository configuration changed and nothing was committed or pushed.
- [2026-07-30 (Codex GPT-5)]: Completed a read-only comparison of the live stack and documented SDR inventory against all 63 current public SDR-Enthusiasts GitHub repositories. The station already covers nearly all relevant two-radio ADS-B/UAT capabilities. The strongest additional use for one of the tested healthy spare orange RTL-SDRs is a VDL2/ACARS station; local ATC audio is also practical, while AIS is technically feasible but needs a dedicated approximately 162 MHz antenna and may have limited inland traffic. No additional RF decoder can share either currently assigned dongle: the degraded blue receiver is occupied by 1090 MHz ADS-B and orange Dongle 1 by 978 MHz UAT. The existing disabled ACARS definitions must not be enabled unchanged because Acarshub conflicts with VRS on host port 8091 and references a nonexistent `tar1090` hostname. Recommended priority remains repairing the degraded 1090 path, confirming a spare orange dongle is physically available, validating the VHF antenna with a continuous known signal, and then running a longer isolated VDL2 trial. Report: `handoff/sdr-enthusiasts-gap-analysis-2026-07-30.md`. No configuration or live state was changed.
- [2026-07-30 (Codex GPT-5)]: Enabled the corrected dedicated FR24 UAT sharing key after Robert updated shared `~/projects/secrets.env`. Backed up live `/opt/adsb/.env` as `/opt/adsb/.env.bak-20260730-fr24-uat-key`, synced only `FR24KEY_UAT`, validated Compose, and recreated only `fr24`. FR24 accepted the key as dedicated UAT station `KLOT60`; the UAT process connected to `dump978:30978`, downloaded its configuration, connected to FR24 over UDP, and reported `working` without the prior ADS-B-key rejection. Final validation: shared/live key values match; primary FR24 station `KLOT9` and UAT station `KLOT60` are both working; status ports 8754 and 8755 return HTTP 200; FR24 and `dump978` are healthy with zero restarts; and the UAT snapshot contained ten aircraft seen within 60 seconds (two direct UAT and eight TIS-B).
- [2026-07-27 (Codex GPT-5)]: Replaced Fly Overhead's non-registering direct Ultrafeeder socket with its official open-source feeder client after Robert approved the workaround and completed the browser pairing. Verified the published installer exactly matched `dhightnm/flyoverhead-feeder` on GitHub before running it. The installer added Node.js 20.20.2 and supporting Debian packages, installed feeder version 0.1.1 under `/opt/fly-overhead-feeder`, stored its paired identity/API credential under `/var/lib/fly-overhead-feeder/`, and enabled `fly-overhead-feeder.service`. The service is paired to Robert's account as station `KLOT9`, runs in JSON mode against `http://127.0.0.1:8080/data/aircraft.json`, is active/enabled with zero restarts, and has no authentication/API failures. Removed the failed `feed.flyoverhead.com:30004` direct connector and its now-obsolete UUID from the repository, shared secrets, and live `.env`; recreated only Ultrafeeder. One expected `ECONNRESET` occurred while its JSON endpoint was briefly unavailable during that recreation and did not recur. Final validation: official client active with zero restarts, JSON source reachable, Ultrafeeder healthy with zero restarts, and no duplicate direct feed remains. Live backup before final connector removal: `/opt/adsb/docker-compose.yml.bak-20260727-flyoverhead-official-client`.
- [2026-07-27 (Codex GPT-5)]: Investigated why Fly Overhead's claim page does not recognize the new direct-feed UUID. Confirmed the dedicated value is a valid RFC 4122 v4 UUID, matches exactly between shared and live environment files, is present in readsb's `feed.flyoverhead.com:30004,beast_reduce_plus_out,uuid=...` connector, and transmits data over an established TCP socket. The remote server nevertheless closes every session after exactly 30 seconds and readsb reconnects immediately. Tested `READSB_NET_HEARTBEAT=20` because Ultrafeeder defaults to 35 seconds, but the server still forced the same exact 30-second disconnect despite active bytes sent; this disproved an idle-heartbeat cause. Rolled the ineffective setting back and recreated only Ultrafeeder. Final state: Ultrafeeder healthy with zero restarts and its normal 35-second heartbeat restored; the direct Fly Overhead connector remains configured, but claiming is blocked by Fly Overhead's ingest/registration behavior. Heartbeat-test live backup: `/opt/adsb/docker-compose.yml.bak-20260727-flyoverhead-heartbeat`.
- [2026-07-27 (Codex GPT-5)]: Added Fly Overhead as a permanent direct Ultrafeeder output using `feed.flyoverhead.com:30004` with `beast_reduce_plus_out` and a dedicated UUID. Generated the UUID into shared `~/projects/secrets.env`, synced it to live `/opt/adsb/.env`, added the tracked placeholder to `.env.example`, and synchronized the repository Compose file to the DietPi host. Live backups: `/opt/adsb/.env.bak-20260727-flyoverhead` and `/opt/adsb/docker-compose.yml.bak-20260727-flyoverhead`. Compose validated and only Ultrafeeder was recreated. Final validation found Ultrafeeder healthy with zero restarts, local aircraft reception restored after startup, and a persistent TCP connection established to Fly Overhead. The feed is currently anonymous; Robert must sign in at `https://flyoverhead.com/feeders/claim` and submit the stored `FLYOVERHEAD_UUID` to claim it.
- [2026-07-27 (Codex GPT-5)]: Tested the newly added shared `FR24KEY_UAT` against the live FR24 dual-band feeder without exposing the credential. Backed up `/opt/adsb/.env` as `/opt/adsb/.env.bak-20260727-fr24-uat-key`, synced the value, validated Compose, and recreated only `fr24`. The UAT process loaded the distinct key, listened on port 8755, and connected successfully to `dump978:30978`, but FR24's server rejected it with `ADSB feeds can only be used with ADSB receivers!`, proving FR24 classifies this sharing key as ADS-B/1090 rather than UAT. To avoid leaving the combined container indefinitely in `health: starting`, restored the live `FR24KEY_UAT` to empty from the backup and recreated only `fr24`; the shared secret remains populated pending a corrected UAT-classified key from FR24. Final validation: primary FR24 and `dump978` are healthy with zero restarts, and the primary FR24 network feed is connected and working.
- [2026-07-25 (Codex GPT-5)]: Consolidated and released the accumulated ADS-B stack checkpoint after Robert requested that all pending work be deployed, committed, and pushed. The reference and live Compose files now agree on permanent UAT integration, defunct RadarPlane removal, disabled Airband/OpenWebRX definitions, and the reverted tar1090 marker experiment. Replaced two legacy hardcoded disabled-profile Icecast passwords with `.env` references and documented those names without storing values. Live Compose validated; no running service needed recreation for the final reference-only credential change. Ultrafeeder, `dump978`, PiAware, FR24, and all checked dedicated feeders remained healthy with zero restarts. Live pre-sync backup: `/opt/adsb/docker-compose.yml.bak-20260725-repository-checkpoint`.
- [2026-07-25 (Codex GPT-5)]: Reverted the tar1090 aircraft-marker experiment after Robert clarified that he meant a different set of icons. Removed only `markerSmall=0.72`, `markerBig=0.82`, `outlineWidth=0.55`, and the appended legacy `TypeDesignatorIcons` mappings from the repository and live `/opt/adsb/docker-compose.yml`. Both Compose files validated, only Ultrafeeder was recreated, and the generated tar1090 page no longer contains the override. Ultrafeeder, `dump978`, PiAware, FR24, and all checked dedicated feeders are healthy with zero restarts; UAT reconnected and tar1090 returned HTTP 200 with live aircraft. Live rollback backup: `/opt/adsb/docker-compose.yml.bak-20260725-icon-override-revert`.
- [2026-07-25 (Codex)]: Read-only live UAT performance check found `dump978` healthy with zero container restarts and its UAT input connected to Ultrafeeder; PiAware was also healthy. A 30-second sample decoded 186 messages (6.2/s). The current one-minute population was 39 aircraft: six direct UAT ADS-B and 33 TIS-B rebroadcast tracks. Direct UAT aircraft reached approximately 28.5 nmi (14.6 nmi mean at the snapshot). This is useful, stable service with strong TIS-B population, but materially below the 29.5 messages/s achieved by Dongle 1+C in its best tested physical position. The only decoder warnings in the prior hour occurred during a normal internal autogain process restart (`usb_claim_interface -6`, `PLL not locked`, and temporary localhost reconnects); the service recovered immediately and remained healthy.
- [2026-07-25 (Codex)]: Removed the defunct RadarPlane integration after Robert approved the documented cleanup. Deleted both `adsb,feed.radarplane.com,30001,...` and `mlat,feed.radarplane.com,31090,...` from the repository and live `/opt/adsb/docker-compose.yml`, validated the rendered Compose configuration, and recreated only Ultrafeeder. Live backup: `/opt/adsb/docker-compose.yml.bak-20260725-radarplane-removal`. Post-change validation found no remaining RadarPlane references or DNS-error loop; Ultrafeeder, `dump978`, PiAware, FR24, and the checked dedicated feeder containers were healthy with zero restarts. UAT reconnected immediately and remained active (33 recent aircraft at the final snapshot), while all nine remaining direct ADS-B aggregator outputs established connections. Five remaining MLAT services completed handshakes; PlaneSpotters, HPRadar, and Fly Italy MLAT endpoints were independently unreachable over IPv4 at the validation snapshot, and PlaneSpotters logged timeout/IPv6 `ENETUNREACH`. The Air Traffic also briefly reported 14.5% connection loss. These are independent remote/network endpoint conditions, not effects of the RadarPlane removal.
- [2026-07-25 (Codex)]: Read-only Ultrafeeder version check found the running untagged/default-latest image is fully current. The container uses image ID `sha256:5c705016...`, created 2026-07-19, with embedded upstream revision `b8ef517d`; the current GHCR `latest` amd64 manifest resolves to the same image/config digest, and upstream `main` is still exactly revision `b8ef517d` with zero later commits. Pulling or recreating Ultrafeeder would therefore make no change at this time.
- [2026-07-25 (Codex)]: Read-only morning health check at approximately 08:40 CDT found the restored two-dongle stack operational. All Compose services were running; Ultrafeeder, `dump978`, PiAware, FR24, and the dedicated feeders reported healthy with zero restarts. UAT was now receiving live traffic: 27 aircraft seen within 60 seconds (five direct UAT ADS-B and 22 TIS-B), and PiAware had transitioned from `starting` to healthy. The degraded blue 1090 receiver was still producing about 50.6 accepted messages/s in the latest one-minute statistics, only five local tracks, and about 35.2 nmi maximum range; Ultrafeeder logged 80 USB packet-loss/SDR-PPM timing warnings in the preceding hour despite the VM reporting NTP synchronized, further supporting the known blue hardware/RF-path fault. RadarPlane remains disconnected because `feed.radarplane.com` has no DNS record. This is not repairable with a replacement hostname: upstream Ultrafeeder issue #209 identified RadarPlane as dead, and commit `10c764d` removed it from current documentation/configuration in October 2025. Recommended cleanup, not yet applied: remove both RadarPlane ADS-B and MLAT entries from the repository and live `/opt/adsb/docker-compose.yml`, then recreate only Ultrafeeder and verify the remaining outbound feeds.
- [2026-07-24 (Codex)]: Restored the approved two-dongle production layout after hardware testing. Proxmox VM 102 now has persistent passthroughs for the filtered blue SDR (`usb0`, used by Ultrafeeder for 1090 MHz) and orange Dongle 1 (`usb1`, used by `dump978` for 978 MHz UAT). Recreated the persistent `dump978` service from `/opt/adsb/docker-compose.yml`; `dump978`, Ultrafeeder, FR24, and all eight checked dedicated feeder containers are healthy with zero restarts. Ultrafeeder established its UAT TCP input from `dump978:30978`, and PiAware connected to the same endpoint. No live UAT RF messages were decoded during final validation, consistent with the late hour and Dongle 1+C's currently poor UAT position, so PiAware remains `health: starting` while waiting for UAT traffic. The blue 1090 path is active and feeding but remains severely RF-degraded as established by the antenna crossover tests. Direct Ultrafeeder outbound sockets were present for the configured aggregators except RadarPlane, whose hostname currently returns no DNS address; FR24's primary 1090 feeder is healthy, while direct FR24 UAT still lacks a dedicated UAT key. No temporary hardware-test containers remain.
- [2026-07-24 (Codex)]: Read-only live check at 23:05 CDT confirmed the station is not currently receiving UAT. No `dump978` container exists, host port 30980 is closed, and PiAware repeatedly logs `Connecting to dump978:30978` followed by `Host not found (authoritative)`. Ultrafeeder remains healthy; PiAware is running but still reports `health: starting`. No containers, configuration, or live state were changed.
- [2026-07-24 (Codex)]: Researched Airplanes.live and ADSB.lol integration without touching the live stack. The repository's Ultrafeeder configuration already contains the current supported ADS-B and MLAT endpoints for both, so integration is effectively complete in the reference Compose and the remaining task is read-only live verification. Standalone provider scripts/containers would be redundant; current `--privacy` flags intentionally hide MLAT site visibility, and ADSB.lol aggregate output must never be fed back into the same decoder. Report: `output/airplanes-live-adsb-lol-integration-research-2026-07-24.md`.
- [2026-07-24 (Codex)]: Tested orange Dongle 2 with Robert's VHF antenna for aircraft datalink reception. Local-only `dumpvdl2` monitored 136.650/136.700/136.800/136.975 MHz for exactly five minutes and decoded zero records. Local-only `acarsdec` monitored 130.025/130.450/131.125/131.550 MHz for approximately two minutes in the initial antenna position and another 2m31s after Robert moved it; both positions decoded zero records. Both decoders tuned successfully and logged no USB/decoder errors. The result does not prove the RF path is bad because traffic is bursty, but a continuous NOAA weather-radio or airport ATIS/AWOS signal should be used next to validate the antenna/cable/position before a longer ACARS test. Temporary containers and Dongle 2 passthrough were removed; production Ultrafeeder remains healthy. Report: `handoff/dongle2-vhf-acars-test-2026-07-24.md`.
- [2026-07-24 (Codex)]: Re-tested UAT after Robert repositioned Dongle 1+Antenna C. With the same isolated dump978 settings, the new position received only 57 messages in exactly two minutes (0.47/s), at most two direct UAT aircraft, and zero TIS-B tracks. The prior position received 8,843 messages in five minutes (29.5/s) and a final snapshot with 33 recent TIS-B tracks. The 98.4% rate loss with unchanged hardware/software identifies the new physical position or orientation as unsuitable, likely because it blocks the relevant UAT/TIS-B ground-station path. Temporary decoder and passthrough were removed; Ultrafeeder remains running and persistent `dump978` remains stopped. Report updated at `handoff/orange-sdr-antenna-comparison-2026-07-24.md`.
- [2026-07-24 (Codex)]: Follow-up hardware tests isolated two additional results. First, temporary UAT test 1+C received 8,843 messages in exactly five minutes (29.5/s), with up to 60 aircraft entries and a final live mix of one direct UAT aircraft plus 33 recent TIS-B tracks. This proves UAT works with Dongle 1+C and confirms the former zero-UAT result came from the removed RF path. Second, the filtered blue production dongle remained severely degraded with two known-good antennas: blue+B received 13,728 messages in five minutes (45.8/s, 19 unique aircraft, 27.4 nmi), and blue+C received 4,181 in two minutes before Robert stopped the conclusive test (34.8/s, 13 unique, 28.4 nmi). The same antennas delivered roughly 345-591 messages/s on orange dongles. Blue was at maximum 49.6 dB gain without USB errors, so its SMA input, filter, LNA, or tuner path is faulty. All temporary containers and passthroughs were removed; Ultrafeeder remains running and blue is currently physically connected to Antenna C. Persistent `dump978` remains intentionally stopped. Report: `handoff/orange-sdr-antenna-comparison-2026-07-24.md`.
- [2026-07-24 (Codex)]: Completed final five-minute rotation `1+C`, `2+A`, `3+B`: 1+C received 155,835 messages/144 unique aircraft/116.5 nmi; 2+A 95,841/97/95.9 nmi; 3+B 108,235/113/106.8 nmi. All had zero errors. The completed balanced crossover matrix shows Dongle 1 is healthy and marginally best: adjusted message geometric means were D1 124,885, D3 122,738, D2 114,913 (D1 +1.7% vs D3 and +8.7% vs D2). Antenna C is decisively best: adjusted message means A 103,656, B 108,030, C 157,297 (C +51.7% vs A and +45.6% vs B). Therefore the original zero-message failure was in the removed antenna/cable/connector RF path, not Dongle 1. Temporary decoders and passthroughs were removed; Ultrafeeder remains healthy and `dump978` remains intentionally stopped. Full matrix and analysis: `handoff/orange-sdr-antenna-comparison-2026-07-24.md`.
- [2026-07-24 (Codex)]: Completed five-minute antenna rotation run 2 with `1+B`, `2+C`, and `3+A`, each on a separate baking-sheet ground plane. Results: 1+B 103,638 messages/100 unique aircraft/111.8 nmi; 2+C 140,867/127/128.4 nmi; 3+A 96,357/98/108.0 nmi. Corrected mean RSSI excluding the empty startup sample was -20.60, -16.38, and -18.66 dBFS respectively; all had zero decoder/USB/tuner errors. Antenna C's lead followed it from Dongle 3 to Dongle 2: it contributed 43.2% of run-1 messages and 41.3% of run-2 messages, strongly identifying Antenna C as the dominant factor. Final rotation needed to complete the matrix is `1+C`, `2+A`, `3+B`. Temporary decoders and passthroughs were removed; Ultrafeeder remains healthy and `dump978` remains intentionally stopped. Report updated at `handoff/orange-sdr-antenna-comparison-2026-07-24.md`.
- [2026-07-24 (Codex)]: Established permanent test labels: old orange is Dongle 1, replacement orange from the first comparison is Dongle 2, and the third orange is Dongle 3; their current antennas are A, B, and C respectively. Ran an exact five-minute simultaneous 1090 test with each antenna on its own baking-sheet ground plane. Results: 1+A 120,601 messages/107 unique aircraft/114.9 nmi; 2+B 112,395/101/120.9 nmi; 3+C 177,292/147/162.4 nmi. Mean RSSI was -19.52, -19.94, and -15.57 dBFS respectively; all had zero decoder/USB/tuner errors. Pair 3+C led message volume by 47.0% over 1+A and 57.7% over 2+B. The reversal between 1+A and 2+B versus the close shared-sheet test confirms that test was materially biased by placement/coupling. Temporary decoders and passthroughs were removed; Ultrafeeder remains healthy and `dump978` remains intentionally stopped. Updated report: `handoff/orange-sdr-antenna-comparison-2026-07-24.md`.
- [2026-07-24 (Codex)]: Ran a simultaneous 1090 MHz comparison of the old orange SDR/new antenna pair and replacement orange SDR/matching antenna pair for approximately 7m57s. With identical fixed-gain settings, old received 131,771 messages, 101 sampled unique aircraft, and 78.9 nmi maximum range; new received 174,768 messages (+32.6%), 122 sampled unique aircraft (+20.8%), and 104.3 nmi maximum range. Both ran without tuner/USB errors, proving the old orange SDR works and implicating its previous antenna/RF path. The result compares complete pairs/positions, not dongles alone: the antennas were only about two inches apart on a shared metal sheet, creating likely mutual coupling. A dongle-only crossover test is recommended. Temporary decoders and Proxmox passthroughs were removed; production Ultrafeeder remains healthy and `dump978` remains intentionally stopped. Full report: `handoff/orange-sdr-antenna-comparison-2026-07-24.md`.
- [2026-07-22 (Codex)]: Tested the orange SDR (serial `978`) as a temporary 1090 MHz receiver after Robert attached a 1090 antenna. Over approximately 9.5 minutes it decoded zero raw messages and zero aircraft, while the blue production 1090 receiver remained online and actively tracked aircraft. The orange SDR had also repeatedly reported `R82XX PLL not locked!` under dump978 and RTLSDR-Airband, making an orange SDR/RF-path hardware fault likely. At Robert's request the test was stopped for dongle replacement: the temporary decoder was removed and `dump978` was intentionally left stopped so it will not claim the device during replacement. The production blue 1090 receiver remains healthy.
- [2026-07-22 (Codex)]: Robert confirmed the FR24 account uses the email already stored as `NPM_EMAIL`. Attempted an automated, secret-safe FR24 UAT signup with the current 1.0.57-1 feeder image using the live station location and `dump978` endpoint. FR24's server repeatedly failed at `Validating email/location information` with `Got no response or empty response from the server`, before receiver selection or key issuance. This matches a documented 2026 FR24 server-side signup failure; FR24 support must manually issue a dedicated UAT key. Temporary signup containers/directories were removed. The live `FR24KEY_UAT` remains empty, leaving the primary FR24 feeder healthy; UAT continues through Ultrafeeder and PiAware.
- [2026-07-22 (Codex)]: Restored the Orange SDR (`978`) to permanent UAT service. `openwebrx` is now behind the `donotstart` profile and its persistent configuration was preserved; its live container was stopped and removed. Enabled `dump978`, added `adsb,dump978,30978,uat_in` to Ultrafeeder so UAT is merged into the existing outbound Beast feeds, and configured PiAware's native external UAT relay. Added FR24 UAT host/port wiring and port 8755, but no valid FR24 UAT key exists: both the live `.env` and shared `~/projects/secrets.env` contained invalid placeholders. The live `FR24KEY_UAT` was cleared so the primary FR24 feeder remains healthy; registering a dedicated UAT key requires the email address for Robert's FR24 account. Validation: `dump978`, Ultrafeeder, and primary FR24 are healthy with zero restarts; PiAware is connected to FlightAware, receives Beast data from Ultrafeeder and UAT from `dump978`, and has sent both types upstream. No actual UAT aircraft were present during the initial validation window. Live pre-change Compose backup: `/opt/adsb/docker-compose.yml.bak-20260722-uat-restore`.
- [2026-07-22 (Codex)]: Documented the recommended Raspberry Pi/RTL-SDR attic layout in `handoff/raspberry-pi-attic-adsb-layout.md`, including Fahrenheit-only thermal guidance. Prefer the antenna and RTL-SDR close together with active USB extended to a Pi in conditioned space; a Pi 5 in an attic requires active cooling and temperature/throttling monitoring.
- [2026-07-22 (Antigravity)]: Stopped the `airband` service and enabled the `openwebrx` service to allow manual, interactive radio tuning on the secondary SDR (`978`). Added the `openwebrx` service definition to `docker-compose.yml`, synced to the DietPi VM (`10.10.1.71`), and started the container. Created an admin account; its credential is stored outside tracked documentation. Verified that `openwebrx` is listening on port `8073`.
- [2026-07-20 (Codex)]: Investigated the large June/July decline in maximum aircraft and related graphs1090 statistics. Archived RRD data shows an abrupt receiver degradation around May 22-25 rather than seasonal traffic variation: average positioned aircraft fell 78%, average range 67%, and locally accepted messages 86%. Signals weakened while automatic tuner gain rose to its maximum, indicating approximately 25-30 dB of added RF-path loss. Most likely causes are antenna/coax/connector failure, loss of LNA power, or SDR input failure. Read-only analysis and diagnostic sequence are documented in `handoff/adsb-reception-drop-analysis-2026-07-20.md`; no live state was changed.
- [2026-06-09 (Antigravity)]: Installed and configured `skystats` at `/opt/skystats/` on VM 10.10.1.71. Set vCPUs to 2 via Proxmox VM 102 config and power-cycled VM. Docker containers start cleanly, database migration succeeded, live feeds are polling ultrafeeder (10.10.1.71:8080/data/aircraft.json) and updating PostgreSQL, and the Svelte web UI is listening at http://10.10.1.71:5173/.
- [2026-06-09 (Antigravity)]: Added 4 changes to docker-compose.yml; synced to VM and committed (c46711d):
  1. **OpenSky Network** feeder added (`opensky` container) — needs `OPENSKY_USERNAME` + `OPENSKY_SERIAL` in `/opt/adsb/.env`. Generate serial with one-time docker run command (see commit message).
  2. **Radar1090 UK** feeder added (`radar1090` container) — needs `RADAR1090_STATION_NAME` + `RADAR1090_KEY` in `/opt/adsb/.env`. Request key from mike@tubby.org. NOTE: Radar1090 is UK/Ireland-focused; if location is outside that area, this feeder may not be accepted.
  3. **Prometheus metrics** enabled on ultrafeeder — ports 9273-9274 now exposed. Endpoint: `http://10.10.1.71:9274/metrics`.
  4. **Timelapse1090** enabled on ultrafeeder — accessible at `http://10.10.1.71:8080/timelapse/`.
  - New containers will error/exit until `.env` vars are populated. Existing stack is unaffected.
  - Gap analysis artifact at: `/home/robert/.gemini/antigravity-cli/brain/6aa38ac1-b11f-4a89-a90b-b3939566e8e4/adsb_gap_analysis.md`

- [2026-06-06 (Claude Code)]: Added agent collaboration rules and initialized handoff log.
